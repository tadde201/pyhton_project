SELECT * from "TB1"
  INSERT INTO TB1 (id, Name, Location)
VALUES
  (1, 'Lemma', 'ethiopia'),
  (2, 'mike', 'jordan'),
  (3, 'john', 'doe');
  Create table test(
    id int,
    Name varchar(20),
    Location varchar(20)
  );
  select * from test
    INSERT INTO test (id, Name, Location)
VALUES
  (1, 'Lemma', 'ethiopia'),
  (2, 'mike', 'jordan'),
  (3, 'john', 'doe');
  select * from test
